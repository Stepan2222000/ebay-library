# План реализации (v1 = Mode 1) — поэтапно

Этот документ — **порядок сборки** переписывания библиотеки. Вся проектная логика и
обоснования — в `SPEC.md`; здесь только **что делаем на каждом этапе, чего не делаем,
и в каком порядке**. Везде ссылки на разделы `SPEC.md`.

Порядок этапов — **по зависимостям** (каждый следующий опирается на готовое
предыдущее и проверяем его отдельно). Сначала item-лейн (он валиднее — прогон 1000,
`SPEC.md` §13 — и без браузера), затем общий контракт хранилища, затем каталог-лейн,
затем интеграция. Mode 2 — после v1.

---

## Принцип прогона (важно)

**Перед каждым этапом — сначала терминал, потом код.** Прогоняем допущения этапа на
живом окружении / сохранённом HTML / дампе, и только если прогон подтвердил —
реализуем (правила проекта: `CLAUDE.md`). У каждого этапа ниже есть блок **🔍 Терминал
перед стартом** — что именно проверить до написания кода. Запись в боевую `ebay_data`
до готовности серверных функций — **только в транзакции с ROLLBACK**.

## Границы v1 (что вне работы вообще)

Вне библиотеки (`SPEC.md` §1.1): пул воркеров, очередь задач, переотдача, запуск
браузера и управление прокси, скачивание/хранение фото (S3). Вне v1: **Mode 2**
(прокси+кука для item — `SPEC.md` §9), он этап 11 «позже».

## Допущения (предв. решения — вето в любой момент)

1. **Пакет и ветка** — новый код пишем в **новой папке `ebay_library/`** (как в
   `SPEC.md` §1), в **отдельной git-ветке**. Старый `ebaylib/` **не трогаем** — он
   остаётся источником для **копирования** переиспользуемых модулей (`srp.py`,
   `urls.py`, `http/fx.py`, `html/normalize.py`, `http/description.py`, `store.py`,
   модели каталога, `page_state.py`) и **тест-оракулом** (DOM-`item.py`).
2. **Порядок** — item-лейн раньше каталога (этапы 1–6), каталог 7–9.
3. **Транспорт item** — `curl_cffi` сразу (готовность к Mode 2 TLS, `SPEC.md` §4.1).
   Если на этапе 2 выяснится, что его нет/неудобен — `httpx` (ebaydesc без антибота,
   дамп 1000 гнался `httpx`); решаем по прогону.
4. **`ebay_data`** — правим **сами** (схема и серверные функции в нашей зоне).
5. **Тест-стратегия** — offline-этапы на Mac (фикстуры `tests/fixtures/` + дамп
   `/tmp/items_modules_dump.jsonl`); live-этапы на сервере `2.27.20.221`.
6. **Старый код** — в новый пакет **не переносим** одиночный `run_worker` и
   resume-машинерию `EbaySession` (вместо них — два воркера, `SPEC.md` §6.2/§6.3);
   старый DOM-`item.py` используем только как **тест-оракул** (`SPEC.md` §12).
7. **StageProfiler** (поэтапные замеры времени) — **переносим** на оба лейна с новыми
   ключами этапов (узкие места видеть полезно).

---

## Этап 1 — Модель товара + JSON-first парсер (offline)

`SPEC.md` §4.3 (карта полей), §7.4 (обяз./опц.), §12 (переписываем парсер товара).

**Цель.** Чистая функция `HTML → ItemPage` из встроенной JSON-модели, без сети и
браузера.

**Делаем.**
- Меняем модель: `ItemPage.price_usd` → **`float | None`** (в Mode 1 не парсится,
  `SPEC.md` §7.4).
- Новый парсер товара (вместо DOM): извлечь самый крупный `"modules":{…}` → JSON и
  взять поля **по карте путей из `examples/README.md` (раздел 1)** — title, condition
  (`NewCondition`→`new`, прочее→`other`), item_number, specifics, location, last_updated,
  фото из `PICTURE.mediaList` (→ `s-l1600`, дедуп; **не** из `JSONLD.image`).
- **seller** — регексом `"sellerUserName"` по **полному HTML** (он вне `modules`,
  `SPEC.md` §4.3).
- Поведение по обязательным/опциональным полям и `ParseError` — строго по `SPEC.md`
  §7.4 / §7.

**Не делаем.**
- **Цену и доставку** (`SPEC.md` §4.4) — не парсим вовсе.
- Сеть, браузер, запрос описания (этапы 2–3), пул и запись в БД.

**🔍 Терминал перед стартом.** Прогнать карту путей на дампе
`/tmp/items_modules_dump.jsonl` (покрытие 994–997/995, как в `SPEC.md` §13); сверить
поле-в-поле со старым DOM-`item.py` на `tests/fixtures/`.

**Готово когда.** На дампе 1000 поля извлекаются с покрытием из §13; совпадает с
DOM-оракулом там, где тот не падает; не-листинг (product-hub) даёт явный сигнал, а не
тихий мусор.

---

## Этап 2 — Транспорт товара (ebaydesc)

`SPEC.md` §3.1 (хосты), §4.1 (curl_cffi), §4.2 (порядок запросов), §7.1 (ретраи),
§11.1 (исходы).

**Цель.** HTTP-IO до ebaydesc без браузера, с ретраями и классификацией исхода.

**Делаем.**
- `GET itm.ebaydesc.com/itm/{id}` (основное) и `itmdesc/{id}` (описание) — `SPEC.md`
  §3.1, §4.2.
- Ретраи транзиента (сеть / 5xx / reset / таймаут) до 3 раз (`SPEC.md` §7.1);
  исчерпание → критично (§7.2).
- Классификация исхода (`SPEC.md` §11.1): **листинг 200** / **503 = транзиент**
  (ретрай, не «ended») / **persistent 404** / **product-hub** (200, `<title>… for
  sale online`, нет `sellerUserName`).

**Не делаем.**
- Прокси и куку (Mode 2, `SPEC.md` §9), ZIP.
- Парсинг полей (этап 1), сборку (этап 3).

**🔍 Терминал перед стартом.** Проверить, что `curl_cffi` установлен (иначе `httpx` —
допущение 3); на известных id подтвердить транзиентность 503 (повторный прогон даёт
другой набор) и product-hub `397974415442`.

**Готово когда.** Фетчер на ~100 id отдаёт сырой HTML + статус + корректный класс
исхода; ретраи срабатывают на искусственном таймауте.

---

## Этап 3 — Сборка item-лейна → `ItemPage | ItemEnded`

`SPEC.md` §4.2 (порядок), §4.5 (фото/описание), §7.4 (поля), §7.5 / §11.1 (ended).

**Цель.** Один вызов «id → результат», без БД и без пула.

**Делаем.**
- Порядок `SPEC.md` §4.2: основной запрос; описание тянем **только** при успехе
  основного.
- Парс (этап 1) + текст описания (`SPEC.md` §4.5); пустое описание / нет фото —
  валидно (§4.5, §7.4).
- Недоступность → `ItemEnded` (`SPEC.md` §7.5 / §11.1: persistent 404, product-hub).

**Не делаем.**
- Пул потребителей, адаптив, запись в БД (этапы 4–6).

**🔍 Терминал перед стартом.** End-to-end на ~50 живых id с Mac (ebaydesc открыт);
сверка собранного `ItemPage` с дампом / DOM-оракулом.

**Готово когда.** На выборке id — валидные `ItemPage` / `ItemEnded`; обязательные поля
строго, опциональные — `None` по `SPEC.md` §7.4.

---

## Этап 4 — Адаптивный контроллер конкуренции

`SPEC.md` §8 (алгоритм), §7.3 (хуки backoff/reset).

**Цель.** Переиспользуемый контроллер числа запросов в полёте `C` — **один на оба
лейна**.

**Делаем.**
- Волновой контроллер: меряем throughput, латентность-в-запросе p50 (внутри запроса),
  ошибки (`SPEC.md` §8.1); параметры `C`/окно/шаг/baseline/`lat_rise`/`ε`/`backoff`
  (§8.2); решение после окна (§8.3).
- Хуки для каталога (`SPEC.md` §7.3): `backoff()` (Pardon → `C ×backoff`) и `reset()`
  (смена страницы → `C` в низкий baseline).

**Не делаем.**
- Привязку к лейнам (этапы 6, 9).
- Доводку §8.4 (окно усреднения, адаптивный шаг, мягче backoff) — отдельно, не
  блокирует.

**🔍 Терминал перед стартом.** Юнит-симуляция на синтетическом источнике (латентность
растёт у «колена») — контроллер разгоняется, оседает, `backoff`/`reset` срабатывают.

**Готово когда.** На симуляции находит «колено» по латентности (как §8.3 / §13), не
уходит в разнос.

---

## Этап 5 — Хранилище: правки `ebay_data` + store-клиент

`SPEC.md` §2.3 (владение данными), §5.4 (цена каталога в USD), §12 (правим store/функции).

**Цель.** Финальный **контракт записи** (кто владеет ценой/доставкой) — чтобы воркеры
сразу писали правильно, а не переписывались потом. Поэтому **до** воркеров.

**Делаем.**
- Серверные функции `ebay_data`: `apply_item_snapshot` **перестаёт писать**
  `price_usd` / `shipping_cost` (item-лейн ими не владеет в Mode 1, `SPEC.md` §2.3);
  `apply_catalog_fetch` **пишет `items.price_usd`** (доставка уже идёт в
  `item_shipping`).
- `store.apply_item` / `store.apply_catalog` — payload под новый контракт.

**Не делаем.**
- ZIP-check Mode 2 (`SPEC.md` §9.3); смену драйвера (asyncpg остаётся, `CLAUDE.md`).

**🔍 Терминал перед стартом.** В транзакции **ROLLBACK** прогнать обе функции на
реальных payload; убедиться, что владение совпало с `SPEC.md` §2.3 (item не трогает
цену/доставку; каталог пишет цену + `item_shipping`).

**Готово когда.** ROLLBACK-прогон показывает правильное владение; повторная запись
идемпотентна.

---

## Этап 6 — Item-воркер

`SPEC.md` §6.1 (колбэки), §6.2 (item-воркер), §7.2 (по жёсткому), §8 (адаптив).

**Цель.** Рабочий item-лейн end-to-end (с записью).

**Делаем.**
- `run_item_worker(next_task, task_done, store, *, …)`: пул **N параллельных
  потребителей** (`SPEC.md` §6.2), общий `next_task` (§6.1), сборка (этап 3) → запись
  (этап 5) → `task_done` **строго после записи**.
- Адаптив `C` (этап 4, `SPEC.md` §8).
- «По жёсткому» (`SPEC.md` §7.2): одна критическая ошибка (ParseError / исчерпанный
  транзиент) → **весь пул умирает** (N задач в полёте без `task_done` → оркестратор
  переотдаёт).

**Не делаем.**
- Браузер, каталог, Mode 2.

**🔍 Терминал перед стартом.** Пул на живых id, запись в ROLLBACK; проверить
смерть-семантику (одна критическая валит пул) и что `task_done` строго после записи.

**Готово когда.** Пул тянет id параллельно с адаптивом, пишет (ROLLBACK); критическая
ошибка валит весь пул как в `SPEC.md` §7.2.

---

## Этап 7 — Каталог-сессия: in-page fetch + классификация

`SPEC.md` §5.1 (живая сессия), §5.2 (in-page fetch), §5.3 (классификация + парс +
пагинация).

**Цель.** Получать страницы выдачи `fetch`-ом из живой браузерной страницы и понимать,
что пришло — без DOM-ожидания.

**Делаем.**
- Прогрев главной = живая сессия (`SPEC.md` §5.1).
- In-page fetch SRP с куками `credentials:'include'` (`SPEC.md` §5.2), отдаёт **полный
  серверный HTML**.
- **Классификация тела** через существующий `page_state(response.url + <title>)`,
  **без DOM-якорей** (`SPEC.md` §5.3): SRP → парсим; Pardon/Access Denied/Error Page →
  в обработку (этап 8 / §7).
- Переиспользуем `parse_search_page` и пагинацию (`SPEC.md` §5.3: стоп `<240` /
  fewer-words / кап).

**Не делаем.**
- Контур восстановления (этап 8), пул (этап 9), fx-запись.

**🔍 Терминал перед стартом (сервер).** Подтвердить, что in-page fetch отдаёт **тело
Pardon** и корректный `response.url` при редиректе на challenge; одиночный fetch =
настоящая SRP, `results_count` совпадает.

**Готово когда.** На сервере одиночные in-page fetch отдают реальные SRP, классификация
различает SRP / Pardon / Error.

---

## Этап 8 — Контур восстановления каталога

`SPEC.md` §6.3 (мьютекс-координация), §7.2 (критичные), §7.3 (восстановление в сессии).

**Цель.** Лечить антибот-события в живой сессии, не теряя `task_done`.

**Делаем.**
- Мьютекс-менеджер **общей страницы** (`SPEC.md` §6.3): один swap/reload на всех
  потребителей.
- **Pardon** → reload упавшего URL + `C ×backoff`; **Access Denied** → новая страница
  (`get_page`) + **reset `C`** (`SPEC.md` §7.3). Запросы «в полёте» бросаем и
  переотправляем; прогресс артикула сохраняется.
- **Транспортная смерть page / Error Page / ParseError** → критическая смерть воркера
  (`SPEC.md` §7.2).

**Не делаем.**
- Mode 2.

**🔍 Терминал перед стартом (сервер).** Подтвердить, что **re-goto упавшего URL гасит
Pardon**; единый swap при N потребителях (спровоцировать искусственно).

**Готово когда.** Pardon / Access Denied восстанавливаются в сессии без потери
`task_done`; критические — валят воркер.

---

## Этап 9 — Каталог-воркер

`SPEC.md` §5.4 (fx→USD), §6.3 (каталог-воркер), §8 (адаптив).

**Цель.** Рабочий каталог-лейн end-to-end (с записью).

**Делаем.**
- `run_catalog_worker(get_page, next_task, task_done, store, *, zip)`: **N
  потребителей на общей странице** (`SPEC.md` §6.3); пер-артикул страницы → **fx→USD**
  (§5.4) → запись (этап 5) → `task_done`.
- Адаптив `C` (этап 4) и восстановление (этап 8).

**Не делаем.**
- Mode 2.

**🔍 Терминал перед стартом (сервер).** Пропускная (адаптив до ~`C≈100`, `SPEC.md`
§13), карточки корректны, fx даёт USD.

**Готово когда.** Воркер на сервере держит поток (сотни каталогов/мин), пишет
(ROLLBACK), 0 Pardon на проверенных темпах (`SPEC.md` §13).

---

## Этап 10 — Интеграция + живая валидация + чистка

`SPEC.md` §13 (доказательная база), открытые §11.

**Цель.** Два воркера вместе на сервере; публичный API; удалить старое.

**Делаем.**
- Связать оба воркера, прогон на `2.27.20.221` (каталог — через прокси; item — прямой
  egress).
- Закрыть открытые проверки этапов 7–8; item на объёме; запись в ebay_data (ROLLBACK
  для проверок).
- Публичный API нового пакета (`__init__`: `run_item_worker`, `run_catalog_worker`).
  Старый `run_worker` / `EbaySession` в новый пакет **не переносим**; DOM-`item.py` —
  тест-оракул (`SPEC.md` §12, допущение 6).

**Не делаем.**
- Mode 2.

**🔍 Терминал перед стартом (сервер).** End-to-end оба лейна вместе.

**Готово когда.** Оба лейна работают совместно; данные в БД консистентны по владению
(`SPEC.md` §2.3).

---

## Этап 11 — (позже) Mode 2

`SPEC.md` §9, §11.9 (срок куки), §11.12 (цена в USD через прокси).

**Делаем потом:** прокси + кука `nonsession` для item (§9.1–9.2), цена + доставка из
товара (§9.4), ZIP-check (§9.3), проверка цены-в-USD через US-прокси (§11.12), масштаб
по прокси (§9.5).

**Сейчас НЕ делаем** — v1 = Mode 1.
