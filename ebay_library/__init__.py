"""ebay_library — рерайт воркер-библиотеки парсинга eBay (v1 = Mode 1).

Дизайн — SPEC.md, порядок сборки — PLAN.md. Публичный API (воркеры) добавляется по
мере готовности лейнов (PLAN этапы 6/9). Старый пакет ``ebaylib`` не трогаем — это
источник копируемых модулей и DOM-тест-оракул (SPEC.md §12).
"""
